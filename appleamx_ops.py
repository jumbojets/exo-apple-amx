# AUTOGENERATED FILE. DO NOT EDIT. Regenerate with gen_appleamx_ops.py
from __future__ import annotations
from exo import *
from exo.libs.externs import select
from exo.stdlib.stdlib import stride
from appleamx_pools import APPLE_AMX_POOL_X, APPLE_AMX_POOL_Y, APPLE_AMX_POOL_Z

@instr("AMX_LDX(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldx_f16(dst: [f16][32] @ APPLE_AMX_POOL_X, src: [f16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, ({src_data}), 0);")
def apple_amx_stx_f16(dst: [f16][32] @ DRAM, src: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldy_f16(dst: [f16][32] @ APPLE_AMX_POOL_Y, src: [f16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, ({src_data}), 0);")
def apple_amx_sty_f16(dst: [f16][32] @ DRAM, src: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldz_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, src: [f16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, ({src_data}), 0);")
def apple_amx_stz_f16(dst: [f16][32] @ DRAM, src: [f16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_LDX(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldx_f32(dst: [f32][16] @ APPLE_AMX_POOL_X, src: [f32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, ({src_data}), 0);")
def apple_amx_stx_f32(dst: [f32][16] @ DRAM, src: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldy_f32(dst: [f32][16] @ APPLE_AMX_POOL_Y, src: [f32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, ({src_data}), 0);")
def apple_amx_sty_f32(dst: [f32][16] @ DRAM, src: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldz_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, src: [f32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, ({src_data}), 0);")
def apple_amx_stz_f32(dst: [f32][16] @ DRAM, src: [f32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_LDX(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldx_f64(dst: [f64][8] @ APPLE_AMX_POOL_X, src: [f64][8] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, ({src_data}), 0);")
def apple_amx_stx_f64(dst: [f64][8] @ DRAM, src: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldy_f64(dst: [f64][8] @ APPLE_AMX_POOL_Y, src: [f64][8] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, ({src_data}), 0);")
def apple_amx_sty_f64(dst: [f64][8] @ DRAM, src: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldz_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, src: [f64][8] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, ({src_data}), 0);")
def apple_amx_stz_f64(dst: [f64][8] @ DRAM, src: [f64][8] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_LDX(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldx_i8(dst: [i8][64] @ APPLE_AMX_POOL_X, src: [i8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, ({src_data}), 0);")
def apple_amx_stx_i8(dst: [i8][64] @ DRAM, src: [i8][64] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldy_i8(dst: [i8][64] @ APPLE_AMX_POOL_Y, src: [i8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, ({src_data}), 0);")
def apple_amx_sty_i8(dst: [i8][64] @ DRAM, src: [i8][64] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldz_i8(dst: [i8][64] @ APPLE_AMX_POOL_Z, src: [i8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, ({src_data}), 0);")
def apple_amx_stz_i8(dst: [i8][64] @ DRAM, src: [i8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_LDX(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldx_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_X, src: [ui8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, ({src_data}), 0);")
def apple_amx_stx_ui8(dst: [ui8][64] @ DRAM, src: [ui8][64] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldy_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_Y, src: [ui8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, ({src_data}), 0);")
def apple_amx_sty_ui8(dst: [ui8][64] @ DRAM, src: [ui8][64] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldz_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_Z, src: [ui8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, ({src_data}), 0);")
def apple_amx_stz_ui8(dst: [ui8][64] @ DRAM, src: [ui8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_LDX(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldx_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_X, src: [ui16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, ({src_data}), 0);")
def apple_amx_stx_ui16(dst: [ui16][32] @ DRAM, src: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldy_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Y, src: [ui16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, ({src_data}), 0);")
def apple_amx_sty_ui16(dst: [ui16][32] @ DRAM, src: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldz_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Z, src: [ui16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, ({src_data}), 0);")
def apple_amx_stz_ui16(dst: [ui16][32] @ DRAM, src: [ui16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_LDX(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldx_i32(dst: [i32][16] @ APPLE_AMX_POOL_X, src: [i32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, ({src_data}), 0);")
def apple_amx_stx_i32(dst: [i32][16] @ DRAM, src: [i32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldy_i32(dst: [i32][16] @ APPLE_AMX_POOL_Y, src: [i32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, ({src_data}), 0);")
def apple_amx_sty_i32(dst: [i32][16] @ DRAM, src: [i32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, ({dst_data}), 0);")
def apple_amx_ldz_i32(dst: [i32][16] @ APPLE_AMX_POOL_Z, src: [i32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, ({src_data}), 0);")
def apple_amx_stz_i32(dst: [i32][16] @ DRAM, src: [i32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_FMA16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000000000000ull);")
def apple_amx_fma16_vec(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] += srcy[i] * srcx[i]

@instr("AMX_FMA16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000008000000ull);")
def apple_amx_fma16_vec_mul(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] = srcy[i] * srcx[i]

@instr("AMX_FMA16(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000010000000ull);")
def apple_amx_fma16_vec_addx(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    dst[i] += srcx[i]

@instr("AMX_FMA16(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000018000000ull);")
def apple_amx_fma16_vec_x(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    dst[i] = srcx[i]

@instr("AMX_FMA16(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000020000000ull);")
def apple_amx_fma16_vec_addy(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] += srcy[i]

@instr("AMX_FMA16(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000028000000ull);")
def apple_amx_fma16_vec_y(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] = srcy[i]

@instr("AMX_FMA16(0, 0, ({dst_data}), 0x8000000038000000ull);")
def apple_amx_fma16_vec_zero(dst: [f16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  for i in seq(0, 32):
    dst[i] = 0.0

@instr("AMX_FMA16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x0ull);")
def apple_amx_fma16_mat(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += srcy[i] * srcx[j]

@instr("AMX_FMA16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000ull);")
def apple_amx_fma16_mat_mul(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = srcy[i] * srcx[j]

@instr("AMX_FMA16(0, ({srcx_data}) * 64, ({dst_data}), 0x10000000ull);")
def apple_amx_fma16_mat_addx(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += srcx[j]

@instr("AMX_FMA16(0, ({srcx_data}) * 64, ({dst_data}), 0x18000000ull);")
def apple_amx_fma16_mat_x(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = srcx[j]

@instr("AMX_FMA16(({srcy_data}) * 64, 0, ({dst_data}), 0x20000000ull);")
def apple_amx_fma16_mat_addy(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += srcy[i]

@instr("AMX_FMA16(({srcy_data}) * 64, 0, ({dst_data}), 0x28000000ull);")
def apple_amx_fma16_mat_y(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = srcy[i]

@instr("AMX_FMA16(0, 0, ({dst_data}), 0x38000000ull);")
def apple_amx_fma16_mat_zero(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 1) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = 0.0

@instr("AMX_FMA16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000800000000000ull | (((uint64_t)({n}) & 31) << 41));")
def apple_amx_fma16_vec_n(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y, n: size):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert n <= 32
  for i in seq(0, n):
    dst[i] += srcy[i] * srcx[i]

@instr("AMX_FMA16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x804000000000ull | (((uint64_t)({n}) & 31) << 41) | (((uint64_t)({m}) & 31) << 32));")
def apple_amx_fma16_mat_mn(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y, m: size, n: size):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert m <= 32
  assert n <= 32
  for i in seq(0, m):
    for j in seq(0, n):
      dst[i, j] += srcy[i] * srcx[j]

@instr("AMX_FMS16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000000000000ull);")
def apple_amx_fms16_vec(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] = dst[i] - srcy[i] * srcx[i]

@instr("AMX_FMS16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000008000000ull);")
def apple_amx_fms16_vec_negmul(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] = -(srcy[i] * srcx[i])

@instr("AMX_FMS16(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000010000000ull);")
def apple_amx_fms16_vec_subx(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    dst[i] = dst[i] - srcx[i]

@instr("AMX_FMS16(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000018000000ull);")
def apple_amx_fms16_vec_negx(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    dst[i] = -srcx[i]

@instr("AMX_FMS16(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000020000000ull);")
def apple_amx_fms16_vec_suby(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] = dst[i] - srcy[i]

@instr("AMX_FMS16(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000028000000ull);")
def apple_amx_fms16_vec_negy(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] = -srcy[i]

@instr("AMX_FMS16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x0ull);")
def apple_amx_fms16_mat(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = dst[i, j] - srcy[i] * srcx[j]

@instr("AMX_FMS16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000ull);")
def apple_amx_fms16_mat_negmul(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = -(srcy[i] * srcx[j])

@instr("AMX_FMS16(0, ({srcx_data}) * 64, ({dst_data}), 0x10000000ull);")
def apple_amx_fms16_mat_subx(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = dst[i, j] - srcx[j]

@instr("AMX_FMS16(0, ({srcx_data}) * 64, ({dst_data}), 0x18000000ull);")
def apple_amx_fms16_mat_negx(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = -srcx[j]

@instr("AMX_FMS16(({srcy_data}) * 64, 0, ({dst_data}), 0x20000000ull);")
def apple_amx_fms16_mat_suby(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = dst[i, j] - srcy[i]

@instr("AMX_FMS16(({srcy_data}) * 64, 0, ({dst_data}), 0x28000000ull);")
def apple_amx_fms16_mat_negy(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = -srcy[i]

@instr("AMX_FMS16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000800000000000ull | (((uint64_t)({n}) & 31) << 41));")
def apple_amx_fms16_vec_n(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y, n: size):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert n <= 32
  for i in seq(0, n):
    dst[i] = dst[i] - srcy[i] * srcx[i]

@instr("AMX_FMS16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x804000000000ull | (((uint64_t)({n}) & 31) << 41) | (((uint64_t)({m}) & 31) << 32));")
def apple_amx_fms16_mat_mn(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y, m: size, n: size):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert m <= 32
  assert n <= 32
  for i in seq(0, m):
    for j in seq(0, n):
      dst[i, j] = dst[i, j] - srcy[i] * srcx[j]

@instr("AMX_FMA32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000000000000ull);")
def apple_amx_fma32_vec(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    dst[i] += srcy[i] * srcx[i]

@instr("AMX_FMA32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000008000000ull);")
def apple_amx_fma32_vec_mul(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    dst[i] = srcy[i] * srcx[i]

@instr("AMX_FMA32(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000010000000ull);")
def apple_amx_fma32_vec_addx(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 16):
    dst[i] += srcx[i]

@instr("AMX_FMA32(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000018000000ull);")
def apple_amx_fma32_vec_x(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 16):
    dst[i] = srcx[i]

@instr("AMX_FMA32(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000020000000ull);")
def apple_amx_fma32_vec_addy(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    dst[i] += srcy[i]

@instr("AMX_FMA32(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000028000000ull);")
def apple_amx_fma32_vec_y(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    dst[i] = srcy[i]

@instr("AMX_FMA32(0, 0, ({dst_data}), 0x8000000038000000ull);")
def apple_amx_fma32_vec_zero(dst: [f32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  for i in seq(0, 16):
    dst[i] = 0.0

@instr("AMX_FMA32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x0ull);")
def apple_amx_fma32_mat(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] += srcy[i] * srcx[j]

@instr("AMX_FMA32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000ull);")
def apple_amx_fma32_mat_mul(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = srcy[i] * srcx[j]

@instr("AMX_FMA32(0, ({srcx_data}) * 64, ({dst_data}), 0x10000000ull);")
def apple_amx_fma32_mat_addx(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] += srcx[j]

@instr("AMX_FMA32(0, ({srcx_data}) * 64, ({dst_data}), 0x18000000ull);")
def apple_amx_fma32_mat_x(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = srcx[j]

@instr("AMX_FMA32(({srcy_data}) * 64, 0, ({dst_data}), 0x20000000ull);")
def apple_amx_fma32_mat_addy(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] += srcy[i]

@instr("AMX_FMA32(({srcy_data}) * 64, 0, ({dst_data}), 0x28000000ull);")
def apple_amx_fma32_mat_y(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = srcy[i]

@instr("AMX_FMA32(0, 0, ({dst_data}), 0x38000000ull);")
def apple_amx_fma32_mat_zero(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 1) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = 0.0

@instr("AMX_FMA32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000800000000000ull | (((uint64_t)({n}) & 31) << 41));")
def apple_amx_fma32_vec_n(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y, n: size):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert n <= 16
  for i in seq(0, n):
    dst[i] += srcy[i] * srcx[i]

@instr("AMX_FMA32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x804000000000ull | (((uint64_t)({n}) & 31) << 41) | (((uint64_t)({m}) & 31) << 32));")
def apple_amx_fma32_mat_mn(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y, m: size, n: size):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert m <= 16
  assert n <= 16
  for i in seq(0, m):
    for j in seq(0, n):
      dst[i, j] += srcy[i] * srcx[j]

@instr("AMX_FMS32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000000000000ull);")
def apple_amx_fms32_vec(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    dst[i] = dst[i] - srcy[i] * srcx[i]

@instr("AMX_FMS32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000008000000ull);")
def apple_amx_fms32_vec_negmul(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    dst[i] = -(srcy[i] * srcx[i])

@instr("AMX_FMS32(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000010000000ull);")
def apple_amx_fms32_vec_subx(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 16):
    dst[i] = dst[i] - srcx[i]

@instr("AMX_FMS32(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000018000000ull);")
def apple_amx_fms32_vec_negx(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 16):
    dst[i] = -srcx[i]

@instr("AMX_FMS32(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000020000000ull);")
def apple_amx_fms32_vec_suby(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    dst[i] = dst[i] - srcy[i]

@instr("AMX_FMS32(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000028000000ull);")
def apple_amx_fms32_vec_negy(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    dst[i] = -srcy[i]

@instr("AMX_FMS32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x0ull);")
def apple_amx_fms32_mat(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = dst[i, j] - srcy[i] * srcx[j]

@instr("AMX_FMS32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000ull);")
def apple_amx_fms32_mat_negmul(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = -(srcy[i] * srcx[j])

@instr("AMX_FMS32(0, ({srcx_data}) * 64, ({dst_data}), 0x10000000ull);")
def apple_amx_fms32_mat_subx(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = dst[i, j] - srcx[j]

@instr("AMX_FMS32(0, ({srcx_data}) * 64, ({dst_data}), 0x18000000ull);")
def apple_amx_fms32_mat_negx(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = -srcx[j]

@instr("AMX_FMS32(({srcy_data}) * 64, 0, ({dst_data}), 0x20000000ull);")
def apple_amx_fms32_mat_suby(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = dst[i, j] - srcy[i]

@instr("AMX_FMS32(({srcy_data}) * 64, 0, ({dst_data}), 0x28000000ull);")
def apple_amx_fms32_mat_negy(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = -srcy[i]

@instr("AMX_FMS32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000800000000000ull | (((uint64_t)({n}) & 31) << 41));")
def apple_amx_fms32_vec_n(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y, n: size):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert n <= 16
  for i in seq(0, n):
    dst[i] = dst[i] - srcy[i] * srcx[i]

@instr("AMX_FMS32(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x804000000000ull | (((uint64_t)({n}) & 31) << 41) | (((uint64_t)({m}) & 31) << 32));")
def apple_amx_fms32_mat_mn(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y, m: size, n: size):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert m <= 16
  assert n <= 16
  for i in seq(0, m):
    for j in seq(0, n):
      dst[i, j] = dst[i, j] - srcy[i] * srcx[j]

@instr("AMX_FMA64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000000000000ull);")
def apple_amx_fma64_vec(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    dst[i] += srcy[i] * srcx[i]

@instr("AMX_FMA64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000008000000ull);")
def apple_amx_fma64_vec_mul(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    dst[i] = srcy[i] * srcx[i]

@instr("AMX_FMA64(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000010000000ull);")
def apple_amx_fma64_vec_addx(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 8):
    dst[i] += srcx[i]

@instr("AMX_FMA64(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000018000000ull);")
def apple_amx_fma64_vec_x(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 8):
    dst[i] = srcx[i]

@instr("AMX_FMA64(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000020000000ull);")
def apple_amx_fma64_vec_addy(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    dst[i] += srcy[i]

@instr("AMX_FMA64(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000028000000ull);")
def apple_amx_fma64_vec_y(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    dst[i] = srcy[i]

@instr("AMX_FMA64(0, 0, ({dst_data}), 0x8000000038000000ull);")
def apple_amx_fma64_vec_zero(dst: [f64][8] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  for i in seq(0, 8):
    dst[i] = 0.0

@instr("AMX_FMA64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x0ull);")
def apple_amx_fma64_mat(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] += srcy[i] * srcx[j]

@instr("AMX_FMA64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000ull);")
def apple_amx_fma64_mat_mul(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = srcy[i] * srcx[j]

@instr("AMX_FMA64(0, ({srcx_data}) * 64, ({dst_data}), 0x10000000ull);")
def apple_amx_fma64_mat_addx(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] += srcx[j]

@instr("AMX_FMA64(0, ({srcx_data}) * 64, ({dst_data}), 0x18000000ull);")
def apple_amx_fma64_mat_x(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = srcx[j]

@instr("AMX_FMA64(({srcy_data}) * 64, 0, ({dst_data}), 0x20000000ull);")
def apple_amx_fma64_mat_addy(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] += srcy[i]

@instr("AMX_FMA64(({srcy_data}) * 64, 0, ({dst_data}), 0x28000000ull);")
def apple_amx_fma64_mat_y(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = srcy[i]

@instr("AMX_FMA64(0, 0, ({dst_data}), 0x38000000ull);")
def apple_amx_fma64_mat_zero(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 1) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = 0.0

@instr("AMX_FMA64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000800000000000ull | (((uint64_t)({n}) & 31) << 41));")
def apple_amx_fma64_vec_n(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y, n: size):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert n <= 8
  for i in seq(0, n):
    dst[i] += srcy[i] * srcx[i]

@instr("AMX_FMA64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x804000000000ull | (((uint64_t)({n}) & 31) << 41) | (((uint64_t)({m}) & 31) << 32));")
def apple_amx_fma64_mat_mn(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y, m: size, n: size):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert m <= 8
  assert n <= 8
  for i in seq(0, m):
    for j in seq(0, n):
      dst[i, j] += srcy[i] * srcx[j]

@instr("AMX_FMS64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000000000000ull);")
def apple_amx_fms64_vec(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    dst[i] = dst[i] - srcy[i] * srcx[i]

@instr("AMX_FMS64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000008000000ull);")
def apple_amx_fms64_vec_negmul(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    dst[i] = -(srcy[i] * srcx[i])

@instr("AMX_FMS64(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000010000000ull);")
def apple_amx_fms64_vec_subx(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 8):
    dst[i] = dst[i] - srcx[i]

@instr("AMX_FMS64(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000018000000ull);")
def apple_amx_fms64_vec_negx(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 8):
    dst[i] = -srcx[i]

@instr("AMX_FMS64(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000020000000ull);")
def apple_amx_fms64_vec_suby(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    dst[i] = dst[i] - srcy[i]

@instr("AMX_FMS64(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000028000000ull);")
def apple_amx_fms64_vec_negy(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    dst[i] = -srcy[i]

@instr("AMX_FMS64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x0ull);")
def apple_amx_fms64_mat(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = dst[i, j] - srcy[i] * srcx[j]

@instr("AMX_FMS64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000ull);")
def apple_amx_fms64_mat_negmul(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = -(srcy[i] * srcx[j])

@instr("AMX_FMS64(0, ({srcx_data}) * 64, ({dst_data}), 0x10000000ull);")
def apple_amx_fms64_mat_subx(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = dst[i, j] - srcx[j]

@instr("AMX_FMS64(0, ({srcx_data}) * 64, ({dst_data}), 0x18000000ull);")
def apple_amx_fms64_mat_negx(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = -srcx[j]

@instr("AMX_FMS64(({srcy_data}) * 64, 0, ({dst_data}), 0x20000000ull);")
def apple_amx_fms64_mat_suby(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = dst[i, j] - srcy[i]

@instr("AMX_FMS64(({srcy_data}) * 64, 0, ({dst_data}), 0x28000000ull);")
def apple_amx_fms64_mat_negy(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = -srcy[i]

@instr("AMX_FMS64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000800000000000ull | (((uint64_t)({n}) & 31) << 41));")
def apple_amx_fms64_vec_n(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y, n: size):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert n <= 8
  for i in seq(0, n):
    dst[i] = dst[i] - srcy[i] * srcx[i]

@instr("AMX_FMS64(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x804000000000ull | (((uint64_t)({n}) & 31) << 41) | (((uint64_t)({m}) & 31) << 32));")
def apple_amx_fms64_mat_mn(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y, m: size, n: size):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  assert m <= 8
  assert n <= 8
  for i in seq(0, m):
    for j in seq(0, n):
      dst[i, j] = dst[i, j] - srcy[i] * srcx[j]

@instr("AMX_MAC16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000000000000ull);")
def apple_amx_mac16_vec(dst: [ui16][32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] += srcy[i] * srcx[i]

@instr("AMX_MAC16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000008000000ull);")
def apple_amx_mac16_vec_mul(dst: [ui16][32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] = srcy[i] * srcx[i]

@instr("AMX_MAC16(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000010000000ull);")
def apple_amx_mac16_vec_addx(dst: [ui16][32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    dst[i] += srcx[i]

@instr("AMX_MAC16(0, ({srcx_data}) * 64, ({dst_data}), 0x8000000018000000ull);")
def apple_amx_mac16_vec_x(dst: [ui16][32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    dst[i] = srcx[i]

@instr("AMX_MAC16(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000020000000ull);")
def apple_amx_mac16_vec_addy(dst: [ui16][32] @ APPLE_AMX_POOL_Z, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] += srcy[i]

@instr("AMX_MAC16(({srcy_data}) * 64, 0, ({dst_data}), 0x8000000028000000ull);")
def apple_amx_mac16_vec_y(dst: [ui16][32] @ APPLE_AMX_POOL_Z, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] = srcy[i]

@instr("AMX_MAC16(0, 0, ({dst_data}), 0x8000000038000000ull);")
def apple_amx_mac16_vec_zero(dst: [ui16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  for i in seq(0, 32):
    dst[i] = 0

@instr("AMX_MAC16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x0ull);")
def apple_amx_mac16_mat(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += srcy[i] * srcx[j]

@instr("AMX_MAC16(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x8000000ull);")
def apple_amx_mac16_mat_mul(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = srcy[i] * srcx[j]

@instr("AMX_MAC16(0, ({srcx_data}) * 64, ({dst_data}), 0x10000000ull);")
def apple_amx_mac16_mat_addx(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += srcx[j]

@instr("AMX_MAC16(0, ({srcx_data}) * 64, ({dst_data}), 0x18000000ull);")
def apple_amx_mac16_mat_x(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = srcx[j]

@instr("AMX_MAC16(({srcy_data}) * 64, 0, ({dst_data}), 0x20000000ull);")
def apple_amx_mac16_mat_addy(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += srcy[i]

@instr("AMX_MAC16(({srcy_data}) * 64, 0, ({dst_data}), 0x28000000ull);")
def apple_amx_mac16_mat_y(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = srcy[i]

@instr("AMX_MAC16(0, 0, ({dst_data}), 0x38000000ull);")
def apple_amx_mac16_mat_zero(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 1) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = 0

@instr("AMX_VECFP(0, ({srcx_data}) * 64, ({dst_data}), 0x2880000000000ull);")
def apple_amx_vecfp16_min(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    dst[i] = select(srcx[i], dst[i], srcx[i], dst[i])

@instr("AMX_VECFP(0, ({srcx_data}) * 64, ({dst_data}), 0x3880000000000ull);")
def apple_amx_vecfp16_max(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 32):
    dst[i] = select(dst[i], srcx[i], srcx[i], dst[i])

@instr("AMX_VECFP(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x2080000000000ull);")
def apple_amx_vecfp16_posselect(dst: [f16][32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] = select(0.0, srcx[i], srcy[i], 0.0)

@instr("AMX_MATFP(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x2080000000000ull);")
def apple_amx_matfp16_posselect(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [f16][32] @ APPLE_AMX_POOL_X, srcy: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = select(0.0, srcx[j], srcy[i], 0.0)

@instr("AMX_VECFP(0, ({srcx_data}) * 64, ({dst_data}), 0x2900000000000ull);")
def apple_amx_vecfp32_min(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 16):
    dst[i] = select(srcx[i], dst[i], srcx[i], dst[i])

@instr("AMX_VECFP(0, ({srcx_data}) * 64, ({dst_data}), 0x3900000000000ull);")
def apple_amx_vecfp32_max(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 16):
    dst[i] = select(dst[i], srcx[i], srcx[i], dst[i])

@instr("AMX_VECFP(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x2100000000000ull);")
def apple_amx_vecfp32_posselect(dst: [f32][16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    dst[i] = select(0.0, srcx[i], srcy[i], 0.0)

@instr("AMX_MATFP(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x2100000000000ull);")
def apple_amx_matfp32_posselect(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, srcx: [f32][16] @ APPLE_AMX_POOL_X, srcy: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = select(0.0, srcx[j], srcy[i], 0.0)

@instr("AMX_VECFP(0, ({srcx_data}) * 64, ({dst_data}), 0x29c0000000000ull);")
def apple_amx_vecfp64_min(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 8):
    dst[i] = select(srcx[i], dst[i], srcx[i], dst[i])

@instr("AMX_VECFP(0, ({srcx_data}) * 64, ({dst_data}), 0x39c0000000000ull);")
def apple_amx_vecfp64_max(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  for i in seq(0, 8):
    dst[i] = select(dst[i], srcx[i], srcx[i], dst[i])

@instr("AMX_VECFP(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x21c0000000000ull);")
def apple_amx_vecfp64_posselect(dst: [f64][8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    dst[i] = select(0.0, srcx[i], srcy[i], 0.0)

@instr("AMX_MATFP(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x21c0000000000ull);")
def apple_amx_matfp64_posselect(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, srcx: [f64][8] @ APPLE_AMX_POOL_X, srcy: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = select(0.0, srcx[j], srcy[i], 0.0)

@instr("AMX_VECINT(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x800000000000ull);")
def apple_amx_vecint_ui16_submul(dst: [ui16][32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] = dst[i] - srcy[i] * srcx[i]

@instr("AMX_MATINT(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x800000000000ull);")
def apple_amx_matint_ui16_submul(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = dst[i, j] - srcy[i] * srcx[j]

@instr("AMX_VECINT(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x1000000000000ull);")
def apple_amx_vecint_ui16_addsum(dst: [ui16][32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] += srcy[i] + srcx[i]

@instr("AMX_MATINT(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x1000000000000ull);")
def apple_amx_matint_ui16_addsum(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += srcy[i] + srcx[j]

@instr("AMX_VECINT(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x1800000000000ull);")
def apple_amx_vecint_ui16_subsum(dst: [ui16][32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    dst[i] = dst[i] - (srcy[i] + srcx[i])

@instr("AMX_MATINT(({srcy_data}) * 64, ({srcx_data}) * 64, ({dst_data}), 0x1800000000000ull);")
def apple_amx_matint_ui16_subsum(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, srcx: [ui16][32] @ APPLE_AMX_POOL_X, srcy: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(srcx, 0) == 1
  assert stride(srcy, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = dst[i, j] - (srcy[i] + srcx[j])

@instr("AMX_EXTRX(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 16));")
def apple_amx_extrx_f16(dst: [f16][32] @ APPLE_AMX_POOL_X, src: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 6));")
def apple_amx_extry_f16(dst: [f16][32] @ APPLE_AMX_POOL_Y, src: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x20000000ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 10));")
def apple_amx_extrh_x_f16(dst: [f16][32] @ APPLE_AMX_POOL_X, src: [f16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x8000000004000400ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrh_y_f16(dst: [f16][32] @ APPLE_AMX_POOL_Y, src: [f16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x20000000ull | (((uint64_t)(({c}) * 2 + ({src_data})) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrv_y_f16(dst: [f16][32] @ APPLE_AMX_POOL_Y, src: [f16][32, 32] @ APPLE_AMX_POOL_Z, c: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert c >= 0
  assert c < 32
  for i in seq(0, 32):
    dst[i] = src[i, c]

@instr("AMX_EXTRX(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 16));")
def apple_amx_extrx_f32(dst: [f32][16] @ APPLE_AMX_POOL_X, src: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 6));")
def apple_amx_extry_f32(dst: [f32][16] @ APPLE_AMX_POOL_Y, src: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x10000000ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 10));")
def apple_amx_extrh_x_f32(dst: [f32][16] @ APPLE_AMX_POOL_X, src: [f32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x8000000004004400ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrh_y_f32(dst: [f32][16] @ APPLE_AMX_POOL_Y, src: [f32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x10000000ull | (((uint64_t)(({c}) * 4 + ({src_data})) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrv_y_f32(dst: [f32][16] @ APPLE_AMX_POOL_Y, src: [f32][16, 16] @ APPLE_AMX_POOL_Z, c: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert c >= 0
  assert c < 16
  for i in seq(0, 16):
    dst[i] = src[i, c]

@instr("AMX_EXTRX(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 16));")
def apple_amx_extrx_f64(dst: [f64][8] @ APPLE_AMX_POOL_X, src: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 6));")
def apple_amx_extry_f64(dst: [f64][8] @ APPLE_AMX_POOL_Y, src: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x0ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 10));")
def apple_amx_extrh_x_f64(dst: [f64][8] @ APPLE_AMX_POOL_X, src: [f64][8] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x8000000004000c00ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrh_y_f64(dst: [f64][8] @ APPLE_AMX_POOL_Y, src: [f64][8] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x0ull | (((uint64_t)(({c}) * 8 + ({src_data})) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrv_y_f64(dst: [f64][8] @ APPLE_AMX_POOL_Y, src: [f64][8, 8] @ APPLE_AMX_POOL_Z, c: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert c >= 0
  assert c < 8
  for i in seq(0, 8):
    dst[i] = src[i, c]

@instr("AMX_EXTRX(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 16));")
def apple_amx_extrx_i8(dst: [i8][64] @ APPLE_AMX_POOL_X, src: [i8][64] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 6));")
def apple_amx_extry_i8(dst: [i8][64] @ APPLE_AMX_POOL_Y, src: [i8][64] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x0ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 10));")
def apple_amx_extrh_x_i8(dst: [i8][64] @ APPLE_AMX_POOL_X, src: [i8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x4000400ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrh_y_i8(dst: [i8][64] @ APPLE_AMX_POOL_Y, src: [i8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 16));")
def apple_amx_extrx_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_X, src: [ui8][64] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 6));")
def apple_amx_extry_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_Y, src: [ui8][64] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x0ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 10));")
def apple_amx_extrh_x_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_X, src: [ui8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x4000400ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrh_y_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_Y, src: [ui8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 16));")
def apple_amx_extrx_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_X, src: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 6));")
def apple_amx_extry_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Y, src: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x20000000ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 10));")
def apple_amx_extrh_x_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_X, src: [ui16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x4007c00ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrh_y_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Y, src: [ui16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x20000000ull | (((uint64_t)(({c}) * 2 + ({src_data})) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrv_y_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Y, src: [ui16][32, 32] @ APPLE_AMX_POOL_Z, c: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert c >= 0
  assert c < 32
  for i in seq(0, 32):
    dst[i] = src[i, c]

@instr("AMX_EXTRX(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 16));")
def apple_amx_extrx_i32(dst: [i32][16] @ APPLE_AMX_POOL_X, src: [i32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x8000000ull | (((uint64_t)({src_data}) & 7) << 20) | (((uint64_t)({dst_data}) & 7) << 6));")
def apple_amx_extry_i32(dst: [i32][16] @ APPLE_AMX_POOL_Y, src: [i32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x10000000ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 10));")
def apple_amx_extrh_x_i32(dst: [i32][16] @ APPLE_AMX_POOL_X, src: [i32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRX(0x4004400ull | (((uint64_t)({src_data}) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrh_y_i32(dst: [i32][16] @ APPLE_AMX_POOL_Y, src: [i32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRY(0x10000000ull | (((uint64_t)(({c}) * 4 + ({src_data})) & 63) << 20) | (((uint64_t)(({dst_data}) * 64) & 511) << 0));")
def apple_amx_extrv_y_i32(dst: [i32][16] @ APPLE_AMX_POOL_Y, src: [i32][16, 16] @ APPLE_AMX_POOL_Z, c: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert c >= 0
  assert c < 16
  for i in seq(0, 16):
    dst[i] = src[i, c]
