# AUTOGENERATED FILE. DO NOT EDIT.

from __future__ import annotations

from exo import *

from exo.libs.externs import select

from exo.stdlib.stdlib import stride

from appleamx_externs import fmin, fmax

from appleamx_pools import APPLE_AMX_POOL_X, APPLE_AMX_POOL_Y, APPLE_AMX_POOL_Z

@instr("AMX_LDX(&{src_data}, {dst_data}, 0);")
def apple_amx_ldx_f16(dst: [f16][32] @ APPLE_AMX_POOL_X, src: [f16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, {src_data}, 0);")
def apple_amx_stx_f16(dst: [f16][32] @ DRAM, src: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, {dst_data}, 0);")
def apple_amx_ldy_f16(dst: [f16][32] @ APPLE_AMX_POOL_Y, src: [f16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, {src_data}, 0);")
def apple_amx_sty_f16(dst: [f16][32] @ DRAM, src: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, {dst_data}, 0);")
def apple_amx_ldz_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, src: [f16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, {src_data}, 0);")
def apple_amx_stz_f16(dst: [f16][32] @ DRAM, src: [f16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRX((1ull << 27) | ({src_data}) << 20 | ({dst_data}) << 16);")
def apple_amx_extrx_f16(dst: [f16][32] @ APPLE_AMX_POOL_X, src: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRY((1ull << 27) | ({dst_data}) << 6 | ({src_data}) << 20);")
def apple_amx_extry_f16(dst: [f16][32] @ APPLE_AMX_POOL_Y, src: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRH((1ull << 63) | (1ull << 26) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhx_f16(dst: [f16][32] @ APPLE_AMX_POOL_X, src: [f16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRV((1ull << 63) | (1ull << 26) | (({src_data}) + {col} * 2) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvx_f16(dst: [f16][32] @ APPLE_AMX_POOL_X, src: [f16][32, 32] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 32
  for i in seq(0, 32):
    dst[i] = src[i, col]

@instr("AMX_EXTRH((1ull << 63) | (1ull << 26) | (1ull << 10) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhy_f16(dst: [f16][32] @ APPLE_AMX_POOL_Y, src: [f16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRV((1ull << 63) | (1ull << 26) | (1ull << 10) | (({src_data}) + {col} * 2) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvy_f16(dst: [f16][32] @ APPLE_AMX_POOL_Y, src: [f16][32, 32] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 32
  for i in seq(0, 32):
    dst[i] = src[i, col]

@instr("AMX_LDX(&{src_data}, {dst_data}, 0);")
def apple_amx_ldx_f32(dst: [f32][16] @ APPLE_AMX_POOL_X, src: [f32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, {src_data}, 0);")
def apple_amx_stx_f32(dst: [f32][16] @ DRAM, src: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, {dst_data}, 0);")
def apple_amx_ldy_f32(dst: [f32][16] @ APPLE_AMX_POOL_Y, src: [f32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, {src_data}, 0);")
def apple_amx_sty_f32(dst: [f32][16] @ DRAM, src: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, {dst_data}, 0);")
def apple_amx_ldz_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, src: [f32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, {src_data}, 0);")
def apple_amx_stz_f32(dst: [f32][16] @ DRAM, src: [f32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRX((1ull << 27) | ({src_data}) << 20 | ({dst_data}) << 16);")
def apple_amx_extrx_f32(dst: [f32][16] @ APPLE_AMX_POOL_X, src: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRY((1ull << 27) | ({dst_data}) << 6 | ({src_data}) << 20);")
def apple_amx_extry_f32(dst: [f32][16] @ APPLE_AMX_POOL_Y, src: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRH((1ull << 63) | (8ull << 11) | (1ull << 26) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhx_f32(dst: [f32][16] @ APPLE_AMX_POOL_X, src: [f32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRV((1ull << 63) | (8ull << 11) | (1ull << 26) | (({src_data}) + {col} * 4) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvx_f32(dst: [f32][16] @ APPLE_AMX_POOL_X, src: [f32][16, 16] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 16
  for i in seq(0, 16):
    dst[i] = src[i, col]

@instr("AMX_EXTRH((1ull << 63) | (8ull << 11) | (1ull << 26) | (1ull << 10) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhy_f32(dst: [f32][16] @ APPLE_AMX_POOL_Y, src: [f32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRV((1ull << 63) | (8ull << 11) | (1ull << 26) | (1ull << 10) | (({src_data}) + {col} * 4) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvy_f32(dst: [f32][16] @ APPLE_AMX_POOL_Y, src: [f32][16, 16] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 16
  for i in seq(0, 16):
    dst[i] = src[i, col]

@instr("AMX_LDX(&{src_data}, {dst_data}, 0);")
def apple_amx_ldx_f64(dst: [f64][8] @ APPLE_AMX_POOL_X, src: [f64][8] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, {src_data}, 0);")
def apple_amx_stx_f64(dst: [f64][8] @ DRAM, src: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, {dst_data}, 0);")
def apple_amx_ldy_f64(dst: [f64][8] @ APPLE_AMX_POOL_Y, src: [f64][8] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, {src_data}, 0);")
def apple_amx_sty_f64(dst: [f64][8] @ DRAM, src: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, {dst_data}, 0);")
def apple_amx_ldz_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, src: [f64][8] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, {src_data}, 0);")
def apple_amx_stz_f64(dst: [f64][8] @ DRAM, src: [f64][8] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_EXTRX((1ull << 27) | ({src_data}) << 20 | ({dst_data}) << 16);")
def apple_amx_extrx_f64(dst: [f64][8] @ APPLE_AMX_POOL_X, src: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_EXTRY((1ull << 27) | ({dst_data}) << 6 | ({src_data}) << 20);")
def apple_amx_extry_f64(dst: [f64][8] @ APPLE_AMX_POOL_Y, src: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_EXTRH((1ull << 63) | (1ull << 11) | (1ull << 26) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhx_f64(dst: [f64][8] @ APPLE_AMX_POOL_X, src: [f64][8] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_EXTRV((1ull << 63) | (1ull << 11) | (1ull << 26) | (({src_data}) + {col} * 8) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvx_f64(dst: [f64][8] @ APPLE_AMX_POOL_X, src: [f64][8, 8] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 8
  for i in seq(0, 8):
    dst[i] = src[i, col]

@instr("AMX_EXTRH((1ull << 63) | (1ull << 11) | (1ull << 26) | (1ull << 10) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhy_f64(dst: [f64][8] @ APPLE_AMX_POOL_Y, src: [f64][8] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 8):
    dst[i] = src[i]

@instr("AMX_EXTRV((1ull << 63) | (1ull << 11) | (1ull << 26) | (1ull << 10) | (({src_data}) + {col} * 8) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvy_f64(dst: [f64][8] @ APPLE_AMX_POOL_Y, src: [f64][8, 8] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 8
  for i in seq(0, 8):
    dst[i] = src[i, col]

@instr("AMX_LDX(&{src_data}, {dst_data}, 0);")
def apple_amx_ldx_i8(dst: [i8][64] @ APPLE_AMX_POOL_X, src: [i8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, {src_data}, 0);")
def apple_amx_stx_i8(dst: [i8][64] @ DRAM, src: [i8][64] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, {dst_data}, 0);")
def apple_amx_ldy_i8(dst: [i8][64] @ APPLE_AMX_POOL_Y, src: [i8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, {src_data}, 0);")
def apple_amx_sty_i8(dst: [i8][64] @ DRAM, src: [i8][64] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, {dst_data}, 0);")
def apple_amx_ldz_i8(dst: [i8][64] @ APPLE_AMX_POOL_Z, src: [i8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, {src_data}, 0);")
def apple_amx_stz_i8(dst: [i8][64] @ DRAM, src: [i8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRX((1ull << 27) | ({src_data}) << 20 | ({dst_data}) << 16);")
def apple_amx_extrx_i8(dst: [i8][64] @ APPLE_AMX_POOL_X, src: [i8][64] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRY((1ull << 27) | ({dst_data}) << 6 | ({src_data}) << 20);")
def apple_amx_extry_i8(dst: [i8][64] @ APPLE_AMX_POOL_Y, src: [i8][64] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRH(0 | (1ull << 26) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhx_i8(dst: [i8][64] @ APPLE_AMX_POOL_X, src: [i8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRV(0 | (1ull << 26) | (({src_data}) + {col} * 1) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvx_i8(dst: [i8][64] @ APPLE_AMX_POOL_X, src: [i8][64, 64] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 64
  for i in seq(0, 64):
    dst[i] = src[i, col]

@instr("AMX_EXTRH(0 | (1ull << 26) | (1ull << 10) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhy_i8(dst: [i8][64] @ APPLE_AMX_POOL_Y, src: [i8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRV(0 | (1ull << 26) | (1ull << 10) | (({src_data}) + {col} * 1) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvy_i8(dst: [i8][64] @ APPLE_AMX_POOL_Y, src: [i8][64, 64] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 64
  for i in seq(0, 64):
    dst[i] = src[i, col]

@instr("AMX_LDX(&{src_data}, {dst_data}, 0);")
def apple_amx_ldx_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_X, src: [ui8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, {src_data}, 0);")
def apple_amx_stx_ui8(dst: [ui8][64] @ DRAM, src: [ui8][64] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, {dst_data}, 0);")
def apple_amx_ldy_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_Y, src: [ui8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, {src_data}, 0);")
def apple_amx_sty_ui8(dst: [ui8][64] @ DRAM, src: [ui8][64] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, {dst_data}, 0);")
def apple_amx_ldz_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_Z, src: [ui8][64] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, {src_data}, 0);")
def apple_amx_stz_ui8(dst: [ui8][64] @ DRAM, src: [ui8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRX((1ull << 27) | ({src_data}) << 20 | ({dst_data}) << 16);")
def apple_amx_extrx_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_X, src: [ui8][64] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRY((1ull << 27) | ({dst_data}) << 6 | ({src_data}) << 20);")
def apple_amx_extry_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_Y, src: [ui8][64] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRH(0 | (1ull << 26) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhx_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_X, src: [ui8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRV(0 | (1ull << 26) | (({src_data}) + {col} * 1) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvx_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_X, src: [ui8][64, 64] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 64
  for i in seq(0, 64):
    dst[i] = src[i, col]

@instr("AMX_EXTRH(0 | (1ull << 26) | (1ull << 10) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhy_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_Y, src: [ui8][64] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 64):
    dst[i] = src[i]

@instr("AMX_EXTRV(0 | (1ull << 26) | (1ull << 10) | (({src_data}) + {col} * 1) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvy_ui8(dst: [ui8][64] @ APPLE_AMX_POOL_Y, src: [ui8][64, 64] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 64
  for i in seq(0, 64):
    dst[i] = src[i, col]

@instr("AMX_LDX(&{src_data}, {dst_data}, 0);")
def apple_amx_ldx_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_X, src: [ui16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, {src_data}, 0);")
def apple_amx_stx_ui16(dst: [ui16][32] @ DRAM, src: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, {dst_data}, 0);")
def apple_amx_ldy_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Y, src: [ui16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, {src_data}, 0);")
def apple_amx_sty_ui16(dst: [ui16][32] @ DRAM, src: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, {dst_data}, 0);")
def apple_amx_ldz_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Z, src: [ui16][32] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, {src_data}, 0);")
def apple_amx_stz_ui16(dst: [ui16][32] @ DRAM, src: [ui16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRX((1ull << 27) | ({src_data}) << 20 | ({dst_data}) << 16);")
def apple_amx_extrx_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_X, src: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRY((1ull << 27) | ({dst_data}) << 6 | ({src_data}) << 20);")
def apple_amx_extry_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Y, src: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRH((1ull << 11) | (1ull << 26) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhx_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_X, src: [ui16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRV((1ull << 11) | (1ull << 26) | (({src_data}) + {col} * 2) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvx_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_X, src: [ui16][32, 32] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 32
  for i in seq(0, 32):
    dst[i] = src[i, col]

@instr("AMX_EXTRH((1ull << 11) | (1ull << 26) | (1ull << 10) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhy_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Y, src: [ui16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 32):
    dst[i] = src[i]

@instr("AMX_EXTRV((1ull << 11) | (1ull << 26) | (1ull << 10) | (({src_data}) + {col} * 2) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvy_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Y, src: [ui16][32, 32] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 32
  for i in seq(0, 32):
    dst[i] = src[i, col]

@instr("AMX_LDX(&{src_data}, {dst_data}, 0);")
def apple_amx_ldx_i32(dst: [i32][16] @ APPLE_AMX_POOL_X, src: [i32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STX(&{dst_data}, {src_data}, 0);")
def apple_amx_stx_i32(dst: [i32][16] @ DRAM, src: [i32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_LDY(&{src_data}, {dst_data}, 0);")
def apple_amx_ldy_i32(dst: [i32][16] @ APPLE_AMX_POOL_Y, src: [i32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STY(&{dst_data}, {src_data}, 0);")
def apple_amx_sty_i32(dst: [i32][16] @ DRAM, src: [i32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_LDZ(&{src_data}, {dst_data}, 0);")
def apple_amx_ldz_i32(dst: [i32][16] @ APPLE_AMX_POOL_Z, src: [i32][16] @ DRAM):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_STZ(&{dst_data}, {src_data}, 0);")
def apple_amx_stz_i32(dst: [i32][16] @ DRAM, src: [i32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRX((1ull << 27) | ({src_data}) << 20 | ({dst_data}) << 16);")
def apple_amx_extrx_i32(dst: [i32][16] @ APPLE_AMX_POOL_X, src: [i32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRY((1ull << 27) | ({dst_data}) << 6 | ({src_data}) << 20);")
def apple_amx_extry_i32(dst: [i32][16] @ APPLE_AMX_POOL_Y, src: [i32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRH((8ull << 11) | (1ull << 26) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhx_i32(dst: [i32][16] @ APPLE_AMX_POOL_X, src: [i32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRV((8ull << 11) | (1ull << 26) | (({src_data}) + {col} * 4) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvx_i32(dst: [i32][16] @ APPLE_AMX_POOL_X, src: [i32][16, 16] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 16
  for i in seq(0, 16):
    dst[i] = src[i, col]

@instr("AMX_EXTRH((8ull << 11) | (1ull << 26) | (1ull << 10) | (({src_data})) << 20 | ({dst_data}) * 64);")
def apple_amx_extrhy_i32(dst: [i32][16] @ APPLE_AMX_POOL_Y, src: [i32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  assert stride(src, 0) == 1
  for i in seq(0, 16):
    dst[i] = src[i]

@instr("AMX_EXTRV((8ull << 11) | (1ull << 26) | (1ull << 10) | (({src_data}) + {col} * 4) << 20 | ({dst_data}) * 64);")
def apple_amx_extrvy_i32(dst: [i32][16] @ APPLE_AMX_POOL_Y, src: [i32][16, 16] @ APPLE_AMX_POOL_Z, col: index):
  assert stride(dst, 0) == 1
  assert stride(src, 1) == 1
  assert col >= 0
  assert col < 16
  for i in seq(0, 16):
    dst[i] = src[i, col]

@instr("AMX_FMA16(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), 0);")
def apple_amx_fma_mat_f16(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += y[i] * x[j]

@instr("AMX_FMS16(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), 0);")
def apple_amx_fms_mat_f16(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = dst[i, j] - y[i] * x[j]

@instr("AMX_FMA16(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 27));")
def apple_amx_mul_mat_f16(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = y[i] * x[j]

@instr("AMX_FMA16(0, ({x_data}) * 64, ({dst_data}), (1ull << 28));")
def apple_amx_addx_mat_f16(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += x[j]

@instr("AMX_FMA16(({y_data}) * 64, 0, ({dst_data}), (1ull << 29));")
def apple_amx_addy_mat_f16(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += y[i]

@instr("AMX_FMA16(0, ({x_data}) * 64, ({dst_data}), (1ull << 28) | (1ull << 27));")
def apple_amx_movx_mat_f16(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = x[j]

@instr("AMX_FMA16(({y_data}) * 64, 0, ({dst_data}), (1ull << 29) | (1ull << 27));")
def apple_amx_movy_mat_f16(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = y[i]

@instr("AMX_FMA16(0, 0, ({dst_data}), (1ull << 29) | (1ull << 28) | (1ull << 27));")
def apple_amx_zero_mat_f16(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 1) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = 0.0

@instr("AMX_FMA16(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63));")
def apple_amx_fma_vec_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    dst[i] += y[i] * x[i]

@instr("AMX_FMS16(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63));")
def apple_amx_fms_vec_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    dst[i] = dst[i] - y[i] * x[i]

@instr("AMX_FMA16(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 27));")
def apple_amx_mul_vec_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    dst[i] = y[i] * x[i]

@instr("AMX_FMA16(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 28));")
def apple_amx_addx_vec_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 32):
    dst[i] += x[i]

@instr("AMX_FMA16(({y_data}) * 64, 0, ({dst_data}), (1ull << 63) | (1ull << 29));")
def apple_amx_addy_vec_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    dst[i] += y[i]

@instr("AMX_FMA16(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 28) | (1ull << 27));")
def apple_amx_movx_vec_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 32):
    dst[i] = x[i]

@instr("AMX_FMA16(({y_data}) * 64, 0, ({dst_data}), (1ull << 63) | (1ull << 29) | (1ull << 27));")
def apple_amx_movy_vec_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    dst[i] = y[i]

@instr("AMX_FMA16(0, 0, ({dst_data}), (1ull << 63) | (1ull << 29) | (1ull << 28) | (1ull << 27));")
def apple_amx_zero_vec_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  for i in seq(0, 32):
    dst[i] = 0.0

@instr("AMX_MATFP(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (2ull << 42) | (4ull << 47));")
def apple_amx_select_mat_f16(dst: [f16][32, 32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = select(0.0, x[j], y[i], 0.0)

@instr("AMX_VECFP(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (2ull << 42) | (4ull << 47));")
def apple_amx_select_vec_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X, y: [f16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    dst[i] = select(0.0, x[i], y[i], 0.0)

@instr("AMX_VECFP(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (2ull << 42) | (5ull << 47));")
def apple_amx_min_vec_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 32):
    dst[i] = fmin(x[i], dst[i])

@instr("AMX_VECFP(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (2ull << 42) | (7ull << 47));")
def apple_amx_max_vec_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 32):
    dst[i] = fmax(x[i], dst[i])

@instr("AMX_VECFP(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (2ull << 42) | (1ull << 38) | (uint64_t){n} << 32);")
def apple_amx_fma_vec_lane_f16(dst: [f16][32] @ APPLE_AMX_POOL_Z, x: [f16][32] @ APPLE_AMX_POOL_X, y: [f16][32] @ APPLE_AMX_POOL_Y, n: index):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  assert n >= 0
  assert n < 32
  for i in seq(0, 32):
    dst[i] += x[i] * y[n]

@instr("AMX_FMA32(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), 0);")
def apple_amx_fma_mat_f32(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] += y[i] * x[j]

@instr("AMX_FMS32(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), 0);")
def apple_amx_fms_mat_f32(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = dst[i, j] - y[i] * x[j]

@instr("AMX_FMA32(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 27));")
def apple_amx_mul_mat_f32(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = y[i] * x[j]

@instr("AMX_FMA32(0, ({x_data}) * 64, ({dst_data}), (1ull << 28));")
def apple_amx_addx_mat_f32(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] += x[j]

@instr("AMX_FMA32(({y_data}) * 64, 0, ({dst_data}), (1ull << 29));")
def apple_amx_addy_mat_f32(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] += y[i]

@instr("AMX_FMA32(0, ({x_data}) * 64, ({dst_data}), (1ull << 28) | (1ull << 27));")
def apple_amx_movx_mat_f32(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = x[j]

@instr("AMX_FMA32(({y_data}) * 64, 0, ({dst_data}), (1ull << 29) | (1ull << 27));")
def apple_amx_movy_mat_f32(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = y[i]

@instr("AMX_FMA32(0, 0, ({dst_data}), (1ull << 29) | (1ull << 28) | (1ull << 27));")
def apple_amx_zero_mat_f32(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 1) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = 0.0

@instr("AMX_FMA32(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63));")
def apple_amx_fma_vec_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    dst[i] += y[i] * x[i]

@instr("AMX_FMS32(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63));")
def apple_amx_fms_vec_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    dst[i] = dst[i] - y[i] * x[i]

@instr("AMX_FMA32(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 27));")
def apple_amx_mul_vec_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    dst[i] = y[i] * x[i]

@instr("AMX_FMA32(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 28));")
def apple_amx_addx_vec_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 16):
    dst[i] += x[i]

@instr("AMX_FMA32(({y_data}) * 64, 0, ({dst_data}), (1ull << 63) | (1ull << 29));")
def apple_amx_addy_vec_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    dst[i] += y[i]

@instr("AMX_FMA32(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 28) | (1ull << 27));")
def apple_amx_movx_vec_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 16):
    dst[i] = x[i]

@instr("AMX_FMA32(({y_data}) * 64, 0, ({dst_data}), (1ull << 63) | (1ull << 29) | (1ull << 27));")
def apple_amx_movy_vec_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    dst[i] = y[i]

@instr("AMX_FMA32(0, 0, ({dst_data}), (1ull << 63) | (1ull << 29) | (1ull << 28) | (1ull << 27));")
def apple_amx_zero_vec_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  for i in seq(0, 16):
    dst[i] = 0.0

@instr("AMX_MATFP(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (4ull << 42) | (4ull << 47));")
def apple_amx_select_mat_f32(dst: [f32][16, 16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    for j in seq(0, 16):
      dst[i, j] = select(0.0, x[j], y[i], 0.0)

@instr("AMX_VECFP(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (4ull << 42) | (4ull << 47));")
def apple_amx_select_vec_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X, y: [f32][16] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 16):
    dst[i] = select(0.0, x[i], y[i], 0.0)

@instr("AMX_VECFP(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (4ull << 42) | (5ull << 47));")
def apple_amx_min_vec_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 16):
    dst[i] = fmin(x[i], dst[i])

@instr("AMX_VECFP(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (4ull << 42) | (7ull << 47));")
def apple_amx_max_vec_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 16):
    dst[i] = fmax(x[i], dst[i])

@instr("AMX_VECFP(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (4ull << 42) | (1ull << 38) | (uint64_t){n} << 32);")
def apple_amx_fma_vec_lane_f32(dst: [f32][16] @ APPLE_AMX_POOL_Z, x: [f32][16] @ APPLE_AMX_POOL_X, y: [f32][16] @ APPLE_AMX_POOL_Y, n: index):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  assert n >= 0
  assert n < 16
  for i in seq(0, 16):
    dst[i] += x[i] * y[n]

@instr("AMX_FMA64(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), 0);")
def apple_amx_fma_mat_f64(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] += y[i] * x[j]

@instr("AMX_FMS64(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), 0);")
def apple_amx_fms_mat_f64(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = dst[i, j] - y[i] * x[j]

@instr("AMX_FMA64(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 27));")
def apple_amx_mul_mat_f64(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = y[i] * x[j]

@instr("AMX_FMA64(0, ({x_data}) * 64, ({dst_data}), (1ull << 28));")
def apple_amx_addx_mat_f64(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] += x[j]

@instr("AMX_FMA64(({y_data}) * 64, 0, ({dst_data}), (1ull << 29));")
def apple_amx_addy_mat_f64(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] += y[i]

@instr("AMX_FMA64(0, ({x_data}) * 64, ({dst_data}), (1ull << 28) | (1ull << 27));")
def apple_amx_movx_mat_f64(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = x[j]

@instr("AMX_FMA64(({y_data}) * 64, 0, ({dst_data}), (1ull << 29) | (1ull << 27));")
def apple_amx_movy_mat_f64(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = y[i]

@instr("AMX_FMA64(0, 0, ({dst_data}), (1ull << 29) | (1ull << 28) | (1ull << 27));")
def apple_amx_zero_mat_f64(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 1) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = 0.0

@instr("AMX_FMA64(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63));")
def apple_amx_fma_vec_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    dst[i] += y[i] * x[i]

@instr("AMX_FMS64(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63));")
def apple_amx_fms_vec_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    dst[i] = dst[i] - y[i] * x[i]

@instr("AMX_FMA64(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 27));")
def apple_amx_mul_vec_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    dst[i] = y[i] * x[i]

@instr("AMX_FMA64(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 28));")
def apple_amx_addx_vec_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 8):
    dst[i] += x[i]

@instr("AMX_FMA64(({y_data}) * 64, 0, ({dst_data}), (1ull << 63) | (1ull << 29));")
def apple_amx_addy_vec_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    dst[i] += y[i]

@instr("AMX_FMA64(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 28) | (1ull << 27));")
def apple_amx_movx_vec_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 8):
    dst[i] = x[i]

@instr("AMX_FMA64(({y_data}) * 64, 0, ({dst_data}), (1ull << 63) | (1ull << 29) | (1ull << 27));")
def apple_amx_movy_vec_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    dst[i] = y[i]

@instr("AMX_FMA64(0, 0, ({dst_data}), (1ull << 63) | (1ull << 29) | (1ull << 28) | (1ull << 27));")
def apple_amx_zero_vec_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  for i in seq(0, 8):
    dst[i] = 0.0

@instr("AMX_MATFP(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (7ull << 42) | (4ull << 47));")
def apple_amx_select_mat_f64(dst: [f64][8, 8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    for j in seq(0, 8):
      dst[i, j] = select(0.0, x[j], y[i], 0.0)

@instr("AMX_VECFP(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (7ull << 42) | (4ull << 47));")
def apple_amx_select_vec_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X, y: [f64][8] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 8):
    dst[i] = select(0.0, x[i], y[i], 0.0)

@instr("AMX_VECFP(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (7ull << 42) | (5ull << 47));")
def apple_amx_min_vec_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 8):
    dst[i] = fmin(x[i], dst[i])

@instr("AMX_VECFP(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (7ull << 42) | (7ull << 47));")
def apple_amx_max_vec_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 8):
    dst[i] = fmax(x[i], dst[i])

@instr("AMX_VECFP(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (7ull << 42) | (1ull << 38) | (uint64_t){n} << 32);")
def apple_amx_fma_vec_lane_f64(dst: [f64][8] @ APPLE_AMX_POOL_Z, x: [f64][8] @ APPLE_AMX_POOL_X, y: [f64][8] @ APPLE_AMX_POOL_Y, n: index):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  assert n >= 0
  assert n < 8
  for i in seq(0, 8):
    dst[i] += x[i] * y[n]

@instr("AMX_MAC16(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), 0);")
def apple_amx_fma_mat_ui16(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, x: [ui16][32] @ APPLE_AMX_POOL_X, y: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += y[i] * x[j]

@instr("AMX_MATINT(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 47));")
def apple_amx_fms_mat_ui16(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, x: [ui16][32] @ APPLE_AMX_POOL_X, y: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = dst[i, j] - y[i] * x[j]

@instr("AMX_MAC16(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 27));")
def apple_amx_mul_mat_ui16(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, x: [ui16][32] @ APPLE_AMX_POOL_X, y: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = y[i] * x[j]

@instr("AMX_MAC16(0, ({x_data}) * 64, ({dst_data}), (1ull << 28));")
def apple_amx_addx_mat_ui16(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, x: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += x[j]

@instr("AMX_MAC16(({y_data}) * 64, 0, ({dst_data}), (1ull << 29));")
def apple_amx_addy_mat_ui16(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, y: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] += y[i]

@instr("AMX_MAC16(0, ({x_data}) * 64, ({dst_data}), (1ull << 28) | (1ull << 27));")
def apple_amx_movx_mat_ui16(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, x: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 1) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = x[j]

@instr("AMX_MAC16(({y_data}) * 64, 0, ({dst_data}), (1ull << 29) | (1ull << 27));")
def apple_amx_movy_mat_ui16(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z, y: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 1) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = y[i]

@instr("AMX_MAC16(0, 0, ({dst_data}), (1ull << 29) | (1ull << 28) | (1ull << 27));")
def apple_amx_zero_mat_ui16(dst: [ui16][32, 32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 1) == 1
  for i in seq(0, 32):
    for j in seq(0, 32):
      dst[i, j] = 0.0

@instr("AMX_MAC16(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63));")
def apple_amx_fma_vec_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Z, x: [ui16][32] @ APPLE_AMX_POOL_X, y: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    dst[i] += y[i] * x[i]

@instr("AMX_VECINT(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 47));")
def apple_amx_fms_vec_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Z, x: [ui16][32] @ APPLE_AMX_POOL_X, y: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    dst[i] = dst[i] - y[i] * x[i]

@instr("AMX_MAC16(({y_data}) * 64, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 27));")
def apple_amx_mul_vec_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Z, x: [ui16][32] @ APPLE_AMX_POOL_X, y: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    dst[i] = y[i] * x[i]

@instr("AMX_MAC16(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 28));")
def apple_amx_addx_vec_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Z, x: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 32):
    dst[i] += x[i]

@instr("AMX_MAC16(({y_data}) * 64, 0, ({dst_data}), (1ull << 63) | (1ull << 29));")
def apple_amx_addy_vec_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Z, y: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    dst[i] += y[i]

@instr("AMX_MAC16(0, ({x_data}) * 64, ({dst_data}), (1ull << 63) | (1ull << 28) | (1ull << 27));")
def apple_amx_movx_vec_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Z, x: [ui16][32] @ APPLE_AMX_POOL_X):
  assert stride(dst, 0) == 1
  assert stride(x, 0) == 1
  for i in seq(0, 32):
    dst[i] = x[i]

@instr("AMX_MAC16(({y_data}) * 64, 0, ({dst_data}), (1ull << 63) | (1ull << 29) | (1ull << 27));")
def apple_amx_movy_vec_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Z, y: [ui16][32] @ APPLE_AMX_POOL_Y):
  assert stride(dst, 0) == 1
  assert stride(y, 0) == 1
  for i in seq(0, 32):
    dst[i] = y[i]

@instr("AMX_MAC16(0, 0, ({dst_data}), (1ull << 63) | (1ull << 29) | (1ull << 28) | (1ull << 27));")
def apple_amx_zero_vec_ui16(dst: [ui16][32] @ APPLE_AMX_POOL_Z):
  assert stride(dst, 0) == 1
  for i in seq(0, 32):
    dst[i] = 0.0
